Реализовано:
Метод get для /users
1. Запрос без параметров возвращает все записи из базы.

2. Запрос с параметрами:
    2.1 Для полей age, salary и join_date можно задавать диапазон от::до, пример:
        age=50::60 - вернет все записи, у которых значение поля age лежит в
                     этом диапазоне, включая границы.
                     Если правая граница диапазона меньше левой, то будут
                     возвращены записи, у которых значение поля равно левой
                     границе.
                     Строка даты принимается в формате iso:
                        2003-12-28T18:18:10-08:00
                        2003-12-29T02:18:10.000+00:00

    2.2 Для остальных полей можно предать несколько значений:
        name=Vasya::Petya - обрабатывается как "или", вернет все записи у
                            которых значение данного поля содержится в строке
                            запроса.

    2.3 Передача значений для некольких полей вернет все записи удовлетворяющие
        значениям переданых параметров.
